# Profile: Firebase

Typical additions before Codex starts:

- pinned Node version
- appropriate Java runtime for Firebase emulators
- Firebase CLI inside Docker
- package lockfile
- emulator ports/services in Compose
- emulator boot verification
- no production deployment credentials in the container unless explicitly required

Verify Node + Java + Firebase CLI versions before Codex handoff.
